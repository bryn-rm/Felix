# Felix Codex Instructions

Before making changes, read `CLAUDE.md` at the repository root.

`CLAUDE.md` contains the project's architecture, development principles, conventions, invariants, testing expectations, and current subsystem guidance. Treat those instructions as applying across the repository unless a more specific `AGENTS.md` overrides them.

Inspect the relevant implementation before modifying it. Do not rely solely on documentation when determining current behavior.
