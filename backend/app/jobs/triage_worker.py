"""
Triage worker — Phase 2.

Handles the AI triage step for a single email within inbox_sync.
"""


async def triage_email(user_id: str, email: dict, settings: dict) -> dict:
    """
    TODO Phase 2: call ai_service.triage_email, return structured triage dict.
    Accepts the user's settings (for vip_contacts list + user_name).
    """
    raise NotImplementedError
